package de.nenick.espressomacchiato.internals

// Hint: When you rename it, it must also be changed inside the build.gradle file. The
// automatic rename process does not recognize the relation yet.
internal annotation class OpenForExtensions