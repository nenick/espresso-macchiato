plugins {
    id 'kotlin-allopen'
    id 'org.jlleitschuh.gradle.ktlint'
}

allOpen {
    annotation("de.nenick.espressomacchiato.internals.OpenForExtensions")
}

dependencies {
    api "androidx.test.espresso:espresso-core"

    androidTestImplementation project(":esp-internal-test-core")
}